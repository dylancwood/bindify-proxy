# Integration test credentials
# Copy to .env.test and fill in values to run integration tests.
# Tests that require these credentials are skipped when the vars are absent.

# Linear API key — generate at https://linear.app/settings/api
LINEAR_TEST_API_KEY=
